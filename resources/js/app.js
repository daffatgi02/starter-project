import './bootstrap';

// Alpine.js is bundled with Livewire — no manual import needed.
// Livewire auto-initializes Alpine when using the @livewireScripts directive.

// Laravel Reverb / Echo — disabled by default.
// Enable by setting REVERB_APP_KEY in .env and uncommenting below:
//
// import Echo from 'laravel-echo';
// import Pusher from 'pusher-js';
// window.Pusher = Pusher;
// window.Echo = new Echo({
//     broadcaster: 'reverb',
//     key: import.meta.env.VITE_REVERB_APP_KEY,
//     wsHost: import.meta.env.VITE_REVERB_HOST,
//     wsPort: import.meta.env.VITE_REVERB_PORT ?? 80,
//     wssPort: import.meta.env.VITE_REVERB_PORT ?? 443,
//     forceTLS: (import.meta.env.VITE_REVERB_SCHEME ?? 'https') === 'https',
//     enabledTransports: ['ws', 'wss'],
// });
